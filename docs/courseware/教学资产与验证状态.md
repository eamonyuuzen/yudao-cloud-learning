# 教学资产与验证状态

> 检查日期：2026-07-30
> 作用：区分“已经运行验证的代码”和“未来阶段的完整设计答案”。

## 1. 当前可直接使用

| 资产 | 状态 | 已有证据 | 开始学习时还要做什么 |
|---|---|---|---|
| 两个月执行总表 | READY | 已按 6B～10 连续业务链拆成 8 周 | 按实际有效日顺延，不按日历强行赶 |
| 6B 逐轮执行包 | READY | 用户入口、导师答案、契约卡和冷启动已分层 | 从手工循环开始，不先打开导师脚本 |
| Java Tool 注册最小测试 | SOURCE-VERIFIED | 不调用模型完成扫描、名称匹配、Schema、callback 调用；1 项通过 | 第 2 日由用户复述机制 |
| Python Agent Lab | RUNNABLE | Python 3.10；OpenAI Agents SDK 0.16.1；32 个测试通过 | 配置临时模型 Key，运行真实查询与评测 |
| 7～10 阶段执行包 | READY | 每阶段含人类逻辑、比喻、代表源码、实验和停止条件 | 到达阶段后重新核对源码位置 |
| 单轮/故障/收尾模板 | READY | 格式和证据槽位已准备 | 每轮只填写事实，不写流水账 |

## 2. 已准备但不能称为已实现

以下文件是 `READY-DESIGN`：

```text
reference-code/step07-transaction-and-claim.md
reference-code/step08-async-job.md
reference-code/step09-redis-protection.md
reference-code/step10-task-event.md
```

它们已经给出未来文件清单、关键方法、SQL、失败分支和测试形状，但目前：

```text
没有写入 Yudao 正式业务包
没有执行数据库迁移
没有通过 Maven 编译
没有产生真实运行证据
```

进入相应阶段后应当按以下顺序使用：

```text
先让用户建模和尝试
→ 对照当时真实源码
→ 只取当前轮必要片段
→ 集成
→ 编译
→ 跑正常与失败实验
→ 才能把状态改成 VERIFIED
```

## 3. Python Agent Lab 的验证边界

已验证：

```text
全部 Python 文件可编译
纯 Python 工具循环可以运行
SDK Agent、Runner、function_tool、RunContextWrapper 可导入
工具 JSON Schema 限制 status、page_no、page_size
Yudao CommonResult 能拆包
空响应 HTTP 401/403/500 与业务 401/403 能稳定翻译
后端畸形 code/page 数据能变成稳定 BACKEND_ERROR
无 Token 时不会发出 Yudao HTTP 请求
FastAPI 必填 Header、Bearer 形状和入口 Session 验证边界
无效 Session 不会消耗模型调用
Session 必须包含合法正数 user.id
每请求最多一次业务工具调用，协议中断可稳定映射为可重试失败
参数、内部异常和工具超时都进入结构化错误与本地 trace
工具 Schema 不含 Token/tenant，返回不含 description
假模型完整 Runner 两轮证明 Token 不进入模型输入、允许字段会进入下一轮
Eval 会比较 expected_arguments，默认不保存完整答案，失败返回非零
32 个不调用真实模型的自动测试通过
```

尚未验证：

```text
真实模型是否稳定选择正确工具
所选 Provider 是否完整支持 Tool Calling
当前电脑到 Yudao Gateway 的真实请求
正常账号和无权限账号的 200/403 对照
固定评测集重复三次的稳定性
```

因此目前准确状态是：

```text
脚手架可运行
≠
第 6B 步已经完成
```

## 4. 依赖版本边界

当前隔离虚拟环境实际安装：

```text
Python 3.10
openai-agents 0.16.1
openai 2.40.0
FastAPI 0.141.1
HTTPX 0.28.1
Pydantic 2.13.4
```

`pyproject.toml` 将 Agents SDK 限定在 `0.16.x`，并临时固定
`openai==2.40.0`。已经复现 `openai 2.49.0` 导入成功、但创建 SDK Usage 时
因 token-detail 字段不兼容而失败。真正进入第 6B 步时：

```text
先重建 .venv
→ 安装
→ 跑 32 个测试
→ 再决定是否升级 SDK
```

## 5. 当前第一动作

下次正式学习无需重新规划：

```text
打开 stages/第6B步-Agent逐轮执行包.md
→ 只做第 1 日
→ 运行 examples/manual_agent_loop.py
→ 用自己的话复述谁决定、谁调度、谁执行、谁持有真实数据
→ 再进入 Yudao Java @Tool
```

尚未配置真实模型 Key 并不阻塞第 1～4 日；第 5 日前准备即可。

## 6. 第 7～10 步设计卡边界

2026-07-30 已做三轮源码/教学红队审查并修正：

```text
Step 07：专用 SQL 入口、独立事务并发测试、H2 精确增量
Step 08：Bean 名、Job 所有权/拒绝契约、显式 tenant、恢复边界、H2 Job 表
Step 09：protection/@InEnum、事务代理、真实限流 Key、Job 幂等闭环、H2 唯一键
Step 10：状态机 + CAS、单审计消费者、DuplicateKey、默认 group、pending trim
所有阶段：错误码顺序、第一条测试命令、H2 create/clean 增量已对齐
```

这些卡仍是 `READY-DESIGN`，不是已集成代码。进入对应阶段后必须基于当时分支
编译和运行，不能因文档已审查就直接标 `INTEGRATED`。
